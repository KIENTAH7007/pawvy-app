{
  "name": "pawvy-app",
  "version": "1.0.0",
  "description": "Pawvy Business Management App",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build && cd .. && (cd portal && npm install && npm run build || echo '⚠️  Portal build failed — internal app will still deploy normally.') && (cd pos && npm install && npm run build || echo '⚠️  POS build failed — internal app will still deploy normally.')",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "archiver": "^6.0.2",
    "bcryptjs": "^3.0.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-cron": "^4.6.0",
    "nodemailer": "^9.0.3",
    "sql.js": "^1.14.1",
    "xlsx": "^0.18.5"
  }
}
